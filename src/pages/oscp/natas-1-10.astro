---
import BaseLayout from '../../layouts/BaseLayout.astro';
---
<BaseLayout title="Natas 1–10 — Liu Huikang">
<article class="article"><header class="article-header"><div class="eyebrow">OSCP · OVER THE WIRE</div><h1>Natas 1–10</h1><div class="article-meta"><span>2023-03-08</span><span>WEB SECURITY</span><span>CTF</span></div></header><div class="prose">
<h2>Natas</h2><p>原博客将 Natas 作为 Web 安全基础靶场进行记录。</p>
<h2>Natas 0</h2><p>第一关直接查看页面源码即可发现密码。</p><pre><code>&lt;!-- password is exposed in the HTML source --&gt;</code></pre>
<h2>Natas 1</h2><p>限制右键并不影响通过开发者工具查看页面源码。</p>
<h2>Natas 2</h2><p>页面中存在文件目录线索，继续枚举可发现 <code>/files</code> 目录及其中的用户信息。</p>
<h2>Natas 3</h2><p>查看 <code>robots.txt</code>：</p><pre><code>User-agent: *
Disallow: /s3cr3t/</code></pre>
<h2>Natas 4</h2><p>根据题目提示修改 HTTP Referer，使请求看起来来自允许的来源。</p>
<h2>迁移说明</h2><p>原文章包含 Natas 1–10 的连续解题记录。本次保留已经确认的核心内容，后续从原始 Markdown 完整恢复每一关，避免凭记忆重写历史记录。</p>
</div></article>
</BaseLayout>
