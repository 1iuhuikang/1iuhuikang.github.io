import { getCollection } from 'astro:content';
export async function GET(){const posts=await getCollection('posts');const urls=['/','/oscp/','/pentest/','/research/','/notes/','/archives/','/tags/','/search/',...posts.map(p=>`/posts/${p.id}/`),...new Set(posts.flatMap(p=>p.data.tags).map(t=>`/tags/${encodeURIComponent(t)}/`))];const body=urls.map(u=>`<url><loc>https://1iuhuikang.github.io${u}</loc></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${body}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}})}
