import { getCollection } from 'astro:content';
export async function GET(){const posts=(await getCollection('posts')).sort((a,b)=>b.data.pubDate.valueOf()-a.data.pubDate.valueOf()).slice(0,20);const esc=(s:string)=>s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');const items=posts.map(p=>`<item><title>${esc(p.data.title)}</title><link>https://1iuhuikang.github.io/posts/${p.id}/</link><guid>https://1iuhuikang.github.io/posts/${p.id}/</guid><pubDate>${p.data.pubDate.toUTCString()}</pubDate><description>${esc(p.data.description)}</description></item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Liu Huikang — Security Research</title><link>https://1iuhuikang.github.io/</link><description>Security research, penetration testing, OSCP notes and technical writeups.</description>${items}</channel></rss>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}})}
